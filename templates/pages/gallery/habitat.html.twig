{% extends 'base.html.twig' %}
{% block title %}Détails de l'habitat {{ habitat.name }}{% endblock %}
{% block body %}
  <section>
    <a href="{{ path('app_gallery_index') }}" class="btn btn-primary m-5">Retour</a>
    <div class="container">
      <h2>Decouvrez l'habitat</h2>
      <div class="card" style="">
        <div class="habitat__image m-auto">
          <img src="{{ vich_uploader_asset(habitat, 'imageFile') }}"  class="card-img-top" alt="Habitat" {{ habitat.name }} "Image" style="width : 700px">
        </div>
        <div class="card-body text-center">
          <h2 class="card-title">{{ habitat.name }}</h2>
          <p class="card-text">{{ habitat.description }}</p>
        </div>
      </div>
    </div>
  </section>
  <section>
    <div class="container m-5 mx-auto text-center">
      <h2>Animals in this habitat</h2>
      <div class="row">
        {{ include('_partials/_animal-cards.html.twig') }}
      </div>
  </section>
  {% endblock %}  